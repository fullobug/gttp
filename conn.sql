CREATE TABLE conns (
    cnid INTEGER,  -- running no
    cnbr INTEGER,  -- bytes read
    cnbs INTEGER,  -- bytes sent
    cnst INTEGER,  -- status code
    cnfd INTEGER,  -- file desc
    cncrdt INTEGER,
    cnupdt INTEGER
);

